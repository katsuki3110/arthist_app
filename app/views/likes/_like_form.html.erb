<% if current_user.present? && sing.likes.find_by(user_id: current_user.id) %>
  <%= link_to 'お気に入り解除', like_path(sing), {method: :delete,
                                                remote: true} %>
<% else %>
  <%= link_to 'お気に入りする', likes_path(sing_id: sing.id), {method: :post,
                                                             remote: true} %>
<% end %>
