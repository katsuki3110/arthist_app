<h1>アーティスト詳細</h1>

<div class="">
  アーティスト名：<%= @arthist.name %>
</div>

<div class="">
  関連リンク：<%= @arthist.link %>
</div>

<ul>
  <li><%= link_to '編集', edit_arthist_path(@arthist) %></li>
  <% if current_user.present? && current_user.admin? %>
    <li><%= link_to '削除', arthist_path(@arthist), data: {confirm: "削除しますか？"} %></li>
  <% end %>
</ul>
